version: '3.4'

services:
  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile
  catalogdb:
    image: postgres
  basketdb:
    image: postgres
  distributedcache:
    image: redis
volumes:
  postgres_volume:
  postgres_basket:

